const state = {
  'ERR_OK': 0
};
const getters = {
  getERR_OK () {
    return this.state.ERR_OK;
  }
};
const actions = {};
const mutations = {};
export default {
  state,
  getters,
  actions,
  mutations
};
